const initialState = {
    movie:[]
}

export const movieReducer = (state = initialState, action) => {
    // switch (type) {

    // case typeName:
    //     return { ...state, ...payload }

    // default:
        return state
    // }


}
